using UnityEngine;

namespace ThirdPerson_InspectSystem
{
    public class ItemInspectorPosition : MonoBehaviour
    {
        //This script is only so the InspectSystem can find the object using the GetComponent<>()
        
        //You need to add this script to the inspect GameObject Position or use the prefab.
        //Add an box collider so the resize of the object can work.
    }
}
